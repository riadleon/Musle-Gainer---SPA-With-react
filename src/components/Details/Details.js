import React, { useState } from 'react';
import Exercise from '../Exercise/Exercise';

const Details = (props) => {
    const {addToExerciseTime } = props;
    console.log(props);
    
    return (
        <div>
             
        </div>
    );
};

export default Details;